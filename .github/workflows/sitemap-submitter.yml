name: Sitemap submitter

on:
  pull_request:
    types: [closed]

jobs:
  merge_job:
    # see https://brennerm.github.io/posts/trigger-github-actions-on-pr-close.html
    if: github.event.pull_request.merged == true && github.event.label.name == 'sitemap'
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo PR #${{ github.event.number }} has been merged

    # steps:
    #   - uses: actions/checkout@v2
    #   - name: Submit sitemap
    #     id: submit-sitemap
    #     uses: repetitive/actions/submit-sitemap@master
    #     with:
    #       agrs: https://www.titaniumringsforever.com/sitemap.xml
